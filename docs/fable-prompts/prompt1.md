做一次性的外部審計，只讀不寫：
 1. 讀這個專案的 http://CLAUDE.md、settings、hooks、skills，找出「文件說的跟程式碼實際做的互相矛盾」、以及「宣稱有自動檢查但實際沒有任何東西在跑」的地方。
 2. 從 ~/.claude/projects/ 的 session log 挖出我最常打斷你、重複糾正你的痛點，附出現次數。
 3. 每個發現輸出一行：現況 → 建議修法 → 一條我能自己跑的驗證指令（grep/test + 期望值）；寫不出驗證指令的發現直接丟掉。
 4. 只給發現清單讓我逐條裁決，不要動任何檔案。
